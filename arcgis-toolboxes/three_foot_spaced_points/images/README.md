# Screenshots go here

Suggested captures:

1. **parameter_form.png** — the tool's dialog in ArcGIS Pro (Geoprocessing pane), showing the input feature class, output paths, and the OBJECTID pair / circle fields filled in.
2. **before_after_map.png** — a map view showing the original input points, next to the same view after running the tool (generated line + spaced points visible).
3. (Optional) **demo.gif** — a short screen recording of running the tool end-to-end.

Delete this file once you've added real images.
